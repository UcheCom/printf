#include <unistd.h>
#include "main.h"
/**
 * _putchar - write the character of c to stdout.
 * @c: character to printout.
 *
 * Return: On success by 1.
 * On error, -1 is returned, and error is set appropriately.
 */


int _putchar(char c)
{
	return (write(1, &c, 1));
}
